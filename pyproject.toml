[project]
name = "tokenhub-server"
version = "0.1.0"
description = "TokenHub exchange server"
requires-python = ">=3.11"
dependencies = ["aiohttp>=3.11.0"]

[project.scripts]
tokenhub-server = "server.main:main"
